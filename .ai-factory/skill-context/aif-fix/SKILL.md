# Project Rules for /aif-fix

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-04-20 12:00
> Based on: 6 analyzed patches and codebase conventions

## Rules

### TryReadFile exception handling
**Source**: winscp-2426-inactive-session-fix.md
**Rule**: Wrap TryReadFile() in try-catch. On exception: if session active, log warning + proceed; if inactive, abort.

### Session active state check
**Source**: winscp-2426-inactive-session-fix.md
**Rule**: Before upload/operation, check FT Terminal->GetActive(). Skip if not active.

### Timestamp comparison pattern
**Source**: winscp-2426-inactive-session-fix.md
**Rule**: For external modification detection, store original timestamp on download, compare with != operator.

### C++ exception handling by reference
**Source**: Codebase convention scan (src/**/*.cpp)
**Rule**: Always catch C++ exceptions by reference (`catch (Exception &E)`), not by value. Log exceptions using `FTerminal->LogEvent()` or `TINYLOG_TRACE` before handling.

### File operation exception handling
**Source**: Codebase pattern (TryReadFile, file operations)
**Rule**: Wrap all file I/O operations in try-catch. On error: log with file path, handle based on session state (abort if inactive, warn if active).
