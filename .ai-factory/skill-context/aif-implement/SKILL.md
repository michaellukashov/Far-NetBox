# Project Rules for /aif-implement

> Auto-generated by `/aif-evolve`. Do not edit manually.
> Last updated: 2026-05-01 16:30
> Based on: 7 analyzed patches and codebase conventions

## Rules

### CMakeLists.txt update rule
**Source**: win32-kitty-input-mode.md, upgrade-openssl-6.5.6.md
**Rule**: When adding new .cpp/.h files, ALWAYS update src/CMakeLists.txt. Missing files = build failure.

### Third-party API integration rule
**Source**: win32-kitty-input-mode.md
**Rule**: Use existing backend_* APIs (PuTTY) without modifying libs/ source. Document API versions used.

### Backward compatibility gate
**Source**: win32-kitty-input-mode.md
**Rule**: New features MUST be gated behind flags, default OFF. Existing behavior must not change.

### WinXP compatibility check
**Source**: win32-kitty-input-mode.md
**Rule**: Avoid Windows 10+ APIs (e.g., ConPTY). Use APIs available since WinXP.

### Multi-phase plan tracking
**Source**: win32-kitty-input-mode.md (13 tasks, 6 phases)
**Rule**: For plans with 10+ tasks, use phase organization and track per-phase completion.

### Configuration persistence rule
**Source**: ftp-heartbeat-noop.md
**Rule**: When adding new config options, verify Save() / Load() / XML serialization.

### logging
**Source**: ARCHITECTURE.md
**Rule**: Add FTerminal->LogEvent() debug logs for all significant code paths (connect, disconnect, errors).

### C++17 feature adoption
**Source**: DESCRIPTION.md (C++17 language requirement)
**Rule**: Use C++17 features where applicable: structured bindings, if constexpr, std::optional, std::filesystem. Avoid C++17 extensions (MSVC-specific).

### Logging standards
**Source**: Codebase logging patterns (TINYLOG_TRACE, LOG_* macros)
**Rule**: Use TINYLOG_TRACE for debug/trace, LOG_INFO for user actions, LOG_ERROR for errors. Always log exception messages before rethrowing.

### Exception safety in implementations
**Source**: Codebase exception patterns
**Rule**: Implementations MUST: (a) use RAII for resource management, (b) catch exceptions at integration points (Far Manager API boundaries), (c) never let exceptions cross Far Manager plugin interface.

### VS path discovery rule
**Source**: 2026-05-01-16.00.md
**Rule**: When creating or modifying Windows batch build scripts, use `vswhere.exe` to dynamically discover the Visual Studio installation path instead of hardcoding edition-specific paths (e.g., `C:\Program Files\Microsoft Visual Studio\2022\Professional\...`). Create a shared helper script (e.g., `find-vs.bat`) that sets a `VCVARSALL` environment variable, and have all build scripts call it before invoking `vcvarsall.bat`.
